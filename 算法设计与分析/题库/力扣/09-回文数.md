# # [回文数](https://leetcode-cn.com/problems/palindrome-number/)

![image-20210416194649735](assets/09-%E5%9B%9E%E6%96%87%E6%95%B0/image-20210416194649735.png)

```
class Solution {
    public boolean isPalindrome(int x) {
        if(x < 0) return false;

//        String s = String.valueOf(x);
//        int n = (int) Math.ceil(s.length()/2);
//        System.out.println(n);
//        for(int i=0;i<n;i++){
//            if(s.charAt(i) != s.charAt(s.length()-i-1)){
//                return false;
//            }
////            System.out.println(s.charAt(i)+":"+s.charAt(s.length()-i-1));
//        }

//      return true;

        int rx = 0;
        int nx = x;
        while(x>0){
            rx = rx*10 + x%10;
            x/=10;
        }
//        System.out.println(rx+":"+nx);
        return rx==nx?true:false;
    }
}
```

