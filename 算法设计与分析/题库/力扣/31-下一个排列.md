# [下一个排列](https://leetcode-cn.com/problems/next-permutation/)

![image-20210510150451916](assets/31-%E4%B8%8B%E4%B8%80%E4%B8%AA%E6%8E%92%E5%88%97/image-20210510150451916.png)

```
class Solution {
 public void nextPermutation(int[] nums) {
        int len = nums.length;
        int maxIndex = len-1;
        List<Integer> df = new ArrayList<>();
        int flag = 0;
        for(int i=len-1;i>0;i--){
            df.add(nums[i]);
            int number = nums[i-1];
            int index = i-1;
            if(nums[i] > nums[i-1]){
                Collections.sort(df);
                // System.out.println(nums[i]+":"+nums[i-1]);
                for(int j=0;j<df.size();j++,i++){
                    nums[i] = df.get(j);
                    if(number < df.get(j) && flag == 0){
                        int temp = df.get(j);
                        nums[i] = nums[index];
                        nums[index] = temp;
                        flag = 1;
                    }
                }
                break;
            }
        }
        if(flag == 0)
            Arrays.sort(nums);
//        for(int i=0;i<nums.length;i++){
//            System.out.println(nums[i]);
//        }
    }
}
```

