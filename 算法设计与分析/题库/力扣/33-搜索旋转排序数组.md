# [搜索旋转排序数组](https://leetcode-cn.com/problems/search-in-rotated-sorted-array/)

![image-20210530132711132](assets/33-%E6%90%9C%E7%B4%A2%E6%97%8B%E8%BD%AC%E6%8E%92%E5%BA%8F%E6%95%B0%E7%BB%84/image-20210530132711132.png)

```
class Solution {
    public int search(int[] nums, int target) {
        int start = -1;
        int len = nums.length;
        int index = -1;
        for(int i=0;i<len;i++){

            if(nums[i] == target && index == -1) {
                index = i;
            }

//            if(nums[i%len] > nums[(i+1)%len] && start == -1){
//                start = (i+1) % len;
//            }
//            i = i % len;
        }

//        System.out.println(index+":"+start);

//        if(index > 0)
//            index = (index+start) % len;

        System.out.println(index);
        return index;
    }
}
```

思路：考虑二分查找，局部有序的也可以使用二分查找。