# [全排列 II](https://leetcode-cn.com/problems/permutations-ii/)

![image-20210603091823628](assets/47-%E5%85%A8%E6%8E%92%E5%88%97%20II/image-20210603091823628.png)

```
    List<List<Integer>> ans = new ArrayList<List<Integer>>();

    public void dfs(int[] nums,int[] labels,List<Integer> list){
        if(list.size() == nums.length){
            ans.add(new ArrayList<Integer>(list));
        }else{
            for(int i=0;i<nums.length;i++){
                if(labels[i] == 1 || (i>0 && nums[i] == nums[i-1] && labels[i-1] == 0)) continue;
                list.add(nums[i]);
                labels[i] = 1;
                dfs(nums,labels,list);
                list.remove(list.size() - 1);
                labels[i] = 0;
            }
        }
    }

    public List<List<Integer>> permuteUnique(int[] nums) {
        int length = nums.length;
        int[] labels = new int[length];

        List<Integer> list = new ArrayList<>();
        Arrays.sort(nums);
        dfs(nums,labels,list);

        return ans;
    }
```

