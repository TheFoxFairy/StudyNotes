# [在排序数组中查找元素的第一个和最后一个位置](https://leetcode-cn.com/problems/find-first-and-last-position-of-element-in-sorted-array/)

![image-20210530133950506](assets/34-%E5%9C%A8%E6%8E%92%E5%BA%8F%E6%95%B0%E7%BB%84%E4%B8%AD%E6%9F%A5%E6%89%BE%E5%85%83%E7%B4%A0%E7%9A%84%E7%AC%AC%E4%B8%80%E4%B8%AA%E5%92%8C%E6%9C%80%E5%90%8E%E4%B8%80%E4%B8%AA%E4%BD%8D%E7%BD%AE/image-20210530133950506.png)

```
class Solution {
    public int searchLeft(int[] nums,int target,int start,int end){

        int i = start, j = end;
        while(i<=j){
            int mid = (j-i) / 2 + i;
            if(nums[mid] < target) i = mid+1;
            else if(nums[mid] > target) j = mid-1;
            else{
                if(mid == 0 || nums[mid-1] != target) return mid;
                else j = mid-1;
            }
        }
        return -1;
    }

    public int searchRight(int[] nums,int target,int start,int end){
        int i = start, j = end;
        while(i<=j){
            int mid = (j-i) / 2 + i;
            if(nums[mid] < target) i = mid+1;
            else if(nums[mid] > target) j = mid-1;
            else{
                if(mid == end || nums[mid+1] != target) return mid;
                else i = mid+1;
            }
        }
        return -1;
    }

    public int[] searchRange(int[] nums, int target) {
        if(nums.length == 0) return new int[]{-1,-1};
        int left = searchLeft(nums,target,0,nums.length-1);
        int right = searchRight(nums,target,0,nums.length-1);
        // System.out.println(left+":"+right);
        return new int[]{left,right};
    }
}
```



