# [删除链表的倒数第 N 个结点](https://leetcode-cn.com/problems/remove-nth-node-from-end-of-list/)

![image-20210421191833032](assets/19-%E5%88%A0%E9%99%A4%E9%93%BE%E8%A1%A8%E7%9A%84%E5%80%92%E6%95%B0%E7%AC%AC%20N%20%E4%B8%AA%E7%BB%93%E7%82%B9/image-20210421191833032.png)

```
public class Solution {
    public ListNode removeNthFromEnd(ListNode head, int n) {

        if(head == null) return null;

        ListNode list_head,list_tail;

        list_head = list_tail = new ListNode();

        List<Integer> list = new ArrayList<>();

        while(head != null){
            list.add(head.val);
            head = head.next;
        }

        list.remove(list.size() - n);
        for(int i=0;i<list.size();i++){
            ListNode node = new ListNode(list.get(i));
            list_tail.next = node;
            list_tail = node;
        }

        list_head = list_head.next;

        // while(list_head != null){
        //     System.out.println(list_head.val);
        //     list_head = list_head.next;
        // }

        return list_head;
    }
}

```

