# 技巧

## 位运算

* 找出有重复数字

```
//通过位运算进行判断是否有重复数字
(1<<number[i]) & flag > 0
```

* 找到第一个未出现最小正数

```
// 将每一个数字看成一个bit，一个int类型数字有32bit位
int[] bit = new int[(len-1) / 32 + 1];
...
int index = (num - 1) / 32;
bit[index] |= (1<<(num-1)%32);
```
